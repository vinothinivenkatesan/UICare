import { Component, OnInit } from '@angular/core';

import { CustomValidators } from 'ng2-validation';

@Component({
  selector: 'validation-form',
  template: require('./validationForm.html')
})
export class ValidationForm  {

}